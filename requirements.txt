# Core ML / fine-tuning stack
torch
transformers
peft
trl
bitsandbytes          # QLoRA 4-bit quantization (verify on Windows before training)
accelerate
datasets
huggingface_hub

# DadaGP decode/render path
pyguitarpro==0.6      # required by the official DadaGP encoder/decoder (newer versions break its Color/API usage)
scipy                 # WAV writing for the dev-time numpy synth

# Experiment tracking (optional but recommended)
wandb

# Utilities
numpy
tqdm

# NOTE: the DadaGP encoder/decoder itself is not a pip package — clone it:
#   git clone https://github.com/dada-bots/dadaGP external/dadaGP
